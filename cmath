#include <iostream>
#include <cmath>

//find more functions at www.cplusplus.com/reference/cmath/
int main(){

    double x = 3;
    double y = 2;
    double g = 3.21;
    double z;

    z = std::max(x,y); //max value
    std::cout<<z<<'\n';
    
    z = std::min(x,y);
    std::cout<<z<<'\n';

    z = pow(2,3);
    std::cout<<z<<'\n';

    z = sqrt(9);
    std::cout<<z<<'\n';

    z =abs(-3);
    std::cout<<z<<'\n';

    z =round(g);
    std::cout<<g;

    z = ceil(g);
    std::cout<<g;

    x = floor(g);
    std::cout<<g;

    return 0;
}
