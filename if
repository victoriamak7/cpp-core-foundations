#include <iostream>

int main(){
    //if statement- do if true, if false dont

    int age;
    std::cout<<"whats your age?: "<<'\n';
    std::cin>>age;

    if (age < 18){
        std::cout<<"you have to be 18 or up, sorry "<<'\n';
    } else if(age == 18){
        std::cout<<"you are welcome to use our site!"<<'\n';
    } else if(age >= 100){
        std::cout<<"arent u too old?"<<'\n';
    }else{
        std::cout<<"welcome!!"<<'\n';
    }

    return 0;
}
